(ns seminarski-rad.core-test
  #_{:clj-kondo/ignore [:refer-all]}
  (:require [clojure.test :refer :all]
            [seminarski-rad.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail. PS: fixed you"
    (is (= 1 1))))

